javac.source=25
javac.target=25
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.test.classes.dir}:\
    ${j2ee.server.platform.classpath}
javac.classpath=\
    ${libs.jakarta.servlet-api.classpath}:\
    ${libs.jakarta.servlet.jsp-api.classpath}:\
    ${libs.mysql-connector-j.classpath}:\
    ${libs.gson.classpath}
j2ee.platform=1.6
j2ee.server.type=Tomcat
annotation.processing.enabled=true
annotation.processing.enabled.in.editor=false
build.web.dir=${build.dir}/web
build.dir=build
dist.dir=dist
dist.war=${dist.dir}/CoffeeBreak.war
web.docroot.dir=web
src.dir=src
